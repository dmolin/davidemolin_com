/*
Index.getLayout = (page) => (
  <OverrideToMainLayout>
    {page}
  </OverrideToMainLayout>
)
*/

import Jumbotron from "/components/Jumbotron";

const Index = () => {
  return (
    <section>
      <Jumbotron />
    </section>
  );
};

export default Index;
